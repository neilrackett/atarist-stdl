*.o
libstdl.a
dist/
# Every host test binary, by pattern rather than by name: the list
# was kept by hand and a new test's binary went into the repository
# because nobody remembered to add it. Same failure as a runner
# with a hand-kept list of tests to run.
tests/host/test_*
!tests/host/test_*.c
tests/host/*.dSYM/
tests/hatari/out/
libstdl-cmini.a
.DS_Store
obj/
tmp/
stdl-examples.zip
